<nav class="w-full font-bold dark:text-white text-black dark:bg-blue-950 bg-blue-100 px-2 py-2">
    <p class="text-lg">
        <a href="/">
            <i class="fa-solid fa-globe-wifi"></i>
            <span class="ml-2">NoIPv6.wtf?</span>
        </a>
    </p>
</nav>
<div class="px-6 py-6 dark:bg-blue-700 dark:text-white">
    <div class="otherSites">
        <div class="header">
            <h1 class="text-xl">Top 100 Websites</h1>
            <p class="text-md">Can these websites be reached via IPv6?</p>
        </div>
        {{#each pingResults as |pingResult i|}}
        <div class="bg-slate-900 mb-2 rounded-4xl p-3">
            <p>{{pingResult.number}}. {{pingResult.domain}}</p><p class="right"><i class="fa-solid fa-circle {{#if pingResult.ipv6}}text-green-600{{else}}text-red-600{{/if}}"></i></p>
        </div>
        {{/each}}
    </div>
    <div class="practiceWhatYouPreach">
        <div class="header">
            <h1>My Websites</h1>
            <p class="remove">Practice what you preach - IPv6 Status of sites I run or help to operate.</p>
        </div>
        {{#each pingResultsMe as |pingResult i|}}
            <div class="pingResult">
                <p>{{pingResult.domain}}</p><p class="right"><i class="fa-solid fa-circle {{#if pingResult.ipv6}}green{{else}}red{{/if}}"></i></p>
            </div>
        {{/each}}
    </div>
</div>