<nav class="w-full font-bold dark:text-white text-black dark:bg-blue-950 bg-blue-100 px-2 py-2">
    <p class="text-lg">
        <a href="/">
            <i class="fa-solid fa-globe-wifi"></i>
            <span class="ml-2">NoIPv6.wtf?</span>
        </a>
    </p>
</nav>
<div class="px-6 py-6 dark:bg-blue-700 dark:text-white flex">
    <div class="w-full lg:w-4/5">
        <div class="header mb-5">
            <h1 class="text-xl">Top 100 Websites</h1>
            <p class="text-md">Can these websites be reached via IPv6?</p>
        </div>
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-2">
            {{#each pingResults as |pingResult i|}}
            <div class="bg-slate-900 rounded-4xl p-3">
                <p><i class="fa-solid fa-circle {{#if pingResult.ipv6}}text-green-600{{else}}text-red-600{{/if}}"></i> {{pingResult.number}}. {{pingResult.domain}}</p>
            </div>
            {{/each}}
        </div>
    </div>
    <div class="practiceWhatYouPreach w-full lg:w-1/5 mt-8 lg:mt-0 lg:ml-6 px-4 lg:px-0">
        <div class="header mb-5">
            <h1 class="text-xl">My Websites</h1>
            <p class="text-md">IPv6 Status of sites I run or help to operate</p>
        </div>
        <div class="grid grid-cols-1 gap-2">
            {{#each pingResultsMe as |pingResult i|}}
            <div class="bg-slate-900 rounded-4xl p-3">
                <p><i class="fa-solid fa-circle {{#if pingResult.ipv6}}text-green-600{{else}}text-red-600{{/if}}"></i> {{pingResult.domain}}</p>
            </div>
            {{/each}}
        </div>
    </div>
</div>